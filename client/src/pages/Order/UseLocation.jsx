// const handleToCart = () => {
//     return () => {
//       const cart = Object.entries(quantities)
//         .filter(([, value]) => value !== "")
//         .map(([id, quantity]) => ({ id, quantity }));
//       navigate("/cart", { state: { cart } });
//     };
//   };